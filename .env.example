# REQUIRED: Agrega tu clientKey de Anti-Captcha aquí (NO pongas una clave real en archivos rastreados por git).
# En local crea un archivo `.env` (que ya está en .gitignore) o define la variable en el entorno de tu servicio.
# Ejemplo:
# ANTI_CAPTCHA_KEY=your_real_client_key_here
ANTI_CAPTCHA_KEY=03ea83a89c837abf30695d43a93c0f29
## Opcional: lista de proxies separadas por comas. Ej: http://user:pass@1.2.3.4:8080,http://1.2.3.5:8080
PROXY_LIST=

# Opcional: URL del servicio DNS/DNI upstream que será consultado si está configurado.
# Ejemplo para hostingviper: https://hostingviper.com/consultas/public/buscar
# Se puede añadir semilla como variable si el upstream requiere: DNI_API_SEED=S0p0rt32025@*
# Ejemplo completo de uso (no añadas la key real al repo):
# DNI_API_URL=https://hostingviper.com/consultas/public/buscar
# DNI_API_SEED=S0p0rt32025@*
DNI_API_URL=https://hostingviper.com/consultas/public/buscar
DNI_API_SEED=S0p0rt32025@*

## Rate limiting para peticiones a /generar-pdf (opcional)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=6

## En producción puedes forzar que ANTI_CAPTCHA_KEY sea obligatoria (true por defecto)
ANTI_CAPTCHA_REQUIRED=true

# Retries / backoff
MAX_RETRIES=3
RETRY_DELAY_MS=1500
BACKOFF_MULT=1.8

# Opcional: usar puppeteer-extra-plugin-recaptcha (true|false)
USE_RECAPTCHA_PLUGIN=false
